schema-version = "1.10.0"

[install]

[vars]

[hook]
on-activate = '''
  # Symlink repos from RogerNavelsaker based on pattern
  for r in ../runtime-intel pi-* context-mode-* code-intel-* repo-map nixos-cli toolflow-mcp; do
    [ -d "$r" ] && ln -sf "$r" .
  done
'''
